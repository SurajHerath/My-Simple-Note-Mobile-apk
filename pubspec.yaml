name: my_simple_note

description: A simple notepad application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  cupertino_icons: ^1.0.2
  get: ^4.6.6
  sqflite: ^2.2.8+4
  path_provider: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true


flutter_icons:
  android: true
  ios: false
  image_path: "C:/Users/suraj/StudioProjects/my_simple_note/notes.png"